#include <iostream>
#include <vector>
#include <fstream>

using namespace std;

int main(){
    string directorio = "dataset/100-300-00";
    int i;
    
        string dirAux = directorio + to_string(i);
        cout << dirAux << endl;
    }
    // ifstream archivo("dataset/100-300-001.txt");
    // vector<string> w;
    // string linea;
    // while (getline(archivo, linea))
    // {
    //     w.push_back(linea);
    // }

    // for (auto it = w.begin(); it != w.end(); it++)
    // {
    //     cout << *it << endl;
    // }
}